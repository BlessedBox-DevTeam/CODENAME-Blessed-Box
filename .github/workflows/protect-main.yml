name: Protect Main

on:
  pull_request:
    branches:
      - main

jobs:
  validate-source:
    runs-on: ubuntu-latest

    steps:
      - name: Verify source branch
        run: |
          if [ "${{ github.head_ref }}" != "development" ]; then
            echo "Only development can merge into main"
            exit 1
          fi
